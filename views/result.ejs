<!DOCTYPE html>
<html>

<head>
    <script src="//ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/4.1.1/js/bootstrap.min.js"></script>
    <script src="//cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <link rel="stylesheet" href="//stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO"
        crossorigin="anonymous">
    <link href="//fonts.googleapis.com/css?family=Play" rel="stylesheet">
    <title>
        <%= requestID %>
    </title>
    <style>
        html,
        body {
            font-family: 'Play';
            background: linear-gradient(to right, #056f6b 0, #15223a 100%)
        }

        .container {
            height: 100%;
            align-content: center;
        }

        a:link {
            color: black; 
            text-decoration:none;
        }

        .card {
            margin-top: auto;
            margin-bottom: auto;
            width: 400px;
            background-color: #16B18E;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="col d-flex justify-content-center">
            <div class="card">
                <div class="card-header">
                    <h3>Result</h3>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-12">
                            <b class="mr-3 pr-3 align-top border-right inline-block align-content-center">TIMESTAMP</b>
                            <p>
                                <%= timestamp %>
                            </p>
                            <br>
                        </div>
                        <div class="col-md-12">
                            <b class="mr-3 pr-3 align-top border-right inline-block align-content-center">REQUEST ID</b>
                            <p>
                                <%= requestID %>
                            </p>
                            <br>
                        </div>
                        <% if (result[0].values.length !== 0) { %>
                        <div class="col-md-12">
                            <b class="mr-3 pr-3 align-top border-right inline-block align-content-center">OUTPUT</b>
                            <p>
                                <% for(var i = 0; i < result[0].values.length; i++){ %>
                                    <%- result[0].values[i] %>   
                                <% } %>
                                <% } %>
                            </p>  
                        </div>
                    </div>
                </div>
                <form action="/">
                    <input type="submit" class="align-content-center" value="New Request" />
                </form>
            </div>
        </div>
    </div>

</html>